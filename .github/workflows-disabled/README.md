# Disabled workflows

Validation and release workflows are parked here while the TypeScript migration begins.
GitHub Actions only discovers workflows in `.github/workflows/`, so these files do not run.
Restore them there only when checks and publishing are explicitly wanted again.
