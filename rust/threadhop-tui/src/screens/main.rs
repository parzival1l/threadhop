//! Main screen — wires session_list (sidebar) + transcript (right pane) +
//! contextual_footer (bottom).
//!
//! Placeholder — implementation lands in Wave C.

#![allow(dead_code)]

use ratatui::Frame;
use crate::app::App;

pub fn draw(_app: &App, _frame: &mut Frame) {
    // Wave C
}
