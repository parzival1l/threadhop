---
description: Bookmark the latest message in the current Claude Code session. Annotate with --note. Bookmarks land in the shared `bookmarks` table that the TUI and CLI also write to.
argument-hint: "[--note <text>]"
disable-model-invocation: true
allowed-tools: Bash(threadhop:*)
---

!`threadhop bookmark $ARGUMENTS`
